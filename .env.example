# Business Gemini Pool 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# API 密钥加密密钥（32字节，生产环境必须设置）
# 生成方式：openssl rand -base64 32 | head -c 32
API_KEY_ENCRYPTION_KEY=your-32-byte-encryption-key-here!!

# 管理员密钥（可选，如果不设置系统会自动生成）
ADMIN_SECRET_KEY=your-admin-secret-key-here

# 服务器配置（可选，默认值已足够）
# SERVER_PORT=8000
# SERVER_HOST=0.0.0.0

# 日志级别（可选，可选值：DEBUG, INFO, ERROR）
# LOG_LEVEL=INFO

# 临时邮箱服务配置（用于自动注册账号）
# TEMPMAIL_WORKER_DOMAIN=tempmail.example.com
# TEMPMAIL_ADMIN_PASSWORD=your-admin-password